<view class="container">
  <view class="header">
    <text class="title">基于大模型的智能教学辅助系统</text>
    <text class="subtitle">提升课堂教学效率，增强学生学习体验</text>
  </view>
  <view class="login-status">
    <block wx:if="{{user_id}}">
      <block wx:if="{{user_role == 'student'}}">
        <text>欢迎, {{user_name}}同学!</text>
      </block>
      <block wx:if="{{user_role == 'teacher'}}">
        <text>欢迎, {{user_name}}老师!</text>
      </block>
      <button bindtap="logout">登出</button>
      <block wx:if="{{user_role == 'student'}}">
        <button bindtap="goToStudentProfile">学生中心</button>
      </block>
      <block wx:if="{{user_role == 'teacher'}}">
        <button bindtap="goToTeacherProfile">查看课程</button>
      </block>
    </block>
    <block wx:else>
      <text>当下的努力，换来的是明天更好的自己！</text>
      <button bindtap="getUserProfile">微信登录</button>
    </block>
  </view>
  <view class="feature-grid">
    <view class="feature-card" wx:for="{{features}}" wx:key="title">
      <text class="feature-title">{{item.title}}</text>
      <text class="feature-description">{{item.description}}</text>
    </view>
  </view>
</view>